from src.cnnClassifier import logger
# You can write this as "cnnClassifier import logger" aswell
# In the setup.py, SRC_REPO = "cnnClassifier" , mentions that the local folder is cnnClassifier

logger.info("Welcome to our custom log")